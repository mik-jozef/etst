/-
  We have the following situation:
  
  `Pair.uniDefList.defList` is a definition list
  that
  - defines an enumeration of expressions such that
  whose definition `theSet` represents the triset
  of all definable trisets.
  
  we would like to show that since `theSet`
-/
